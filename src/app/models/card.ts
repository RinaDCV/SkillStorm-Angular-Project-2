export class Card {
    sign:string;
    symbol:string;
    element:string;

    constructor(sign:string, symbol:string, element:string){
        this.sign = sign;
        this.symbol = symbol;
        this.element =element;
    }
}
